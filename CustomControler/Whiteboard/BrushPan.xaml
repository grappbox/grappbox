<UserControl
    x:Class="GrappBox.CustomControler.BrushPan"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GrappBox.CustomControler"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="150"
    d:DesignWidth="100">
    <ListBox x:Name="BrushListView" Background="WhiteSmoke" SelectionChanged="BrushListView_SelectionChanged">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Horizontal" IsTapEnabled="False" IsDoubleTapEnabled="False" IsHoldingEnabled="False" IsRightTapEnabled="False">
                    <TextBlock Text="{Binding Label}" FontWeight="Bold" FontSize="14.667" />
                    <Line Margin="15,0,0,0" StrokeThickness="{Binding Size}" Stroke="Black" Stretch="Fill" X2="20" VerticalAlignment="Center" />
                </StackPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
