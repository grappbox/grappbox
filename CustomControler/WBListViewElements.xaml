<UserControl
    x:Class="GrappBox.CustomControler.WBListViewElements"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GrappBox.CustomControler"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="80">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Border Grid.RowSpan="3" CornerRadius="5" BorderThickness="2">
            <Border.Background>
                <SolidColorBrush Color="{StaticResource GrayGrappBox}"/>
            </Border.Background>
            <Border.BorderBrush>
                <SolidColorBrush Color="{StaticResource RedGrappBox}"/>
            </Border.BorderBrush>
        </Border>
        <TextBlock Text=""/>
    </Grid>
</UserControl>