<Page
    x:Class="GrappBox.View.UserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GrappBox.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:slideMenu="using:GrappBox.CustomControler.SlidingMenu"
    xmlns:valueconverter="using:GrappBox.ViewModel"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:customControler="using:GrappBox.CustomControler">

    <slideMenu:SlideInMenuContentControl x:Name="slideInMenuContentControl" Grid.Row="1" MenuState="Left" IsDoubleTapEnabled="False">
        <slideMenu:SlideInMenuContentControl.LeftMenuContent>
            <customControler:LeftMenu/>
        </slideMenu:SlideInMenuContentControl.LeftMenuContent>
        <slideMenu:SlideInMenuContentControl.Content>
            <Grid Height="{Binding ActualHeight, ElementName=slideInMenuContentControl, Mode=OneWay}" Width="{Binding ActualWidth, ElementName=slideInMenuContentControl, Mode=OneWay}">
                <Grid.Background>
                    <SolidColorBrush Color="{StaticResource LightWhiteGrappBox}"/>
                </Grid.Background>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="9*"/>
                </Grid.RowDefinitions>
                <ProgressBar VerticalAlignment="Center" Grid.Row="1" x:Name="LoadingBar" Visibility="Collapsed" IsEnabled="False" IsIndeterminate="true" Height="4" HorizontalAlignment="Stretch" Canvas.ZIndex="1"/>
                <Grid Grid.Row="0">
                    <Grid.Background>
                        <SolidColorBrush Color="{StaticResource RedGrappBox}"/>
                    </Grid.Background>
                    <TextBlock VerticalAlignment="Center" Text="User Settings" FontWeight="Bold" FontSize="25"/>
                </Grid>
                <ScrollViewer Grid.Row="1">
                    <StackPanel>
                        <StackPanel.Resources>
                            <valueconverter:DateTimeToDateConverter x:Key="DateConverter"/>
                        </StackPanel.Resources>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Firstname" FontFamily="Segoe WP" FontSize="15"/>
                        <TextBox x:Name="FirstName" PlaceholderText="Firstname" Text="{Binding Firstname, Mode=TwoWay}"/>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Lastname" FontFamily="Segoe WP" FontSize="15"/>
                        <TextBox x:Name="Lastname" PlaceholderText="Lastname" Text="{Binding Lastname, Mode=TwoWay}"/>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Birthday" FontFamily="Segoe WP" FontSize="15"/>
                        <DatePicker Date="{Binding Birthday, Converter={StaticResource DateConverter}}"/>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Avatar" FontFamily="Segoe WP" FontSize="15"/>
                        <Grid>
                            <Image x:Name="img" Source="{Binding Avatar, Mode=OneWay}" HorizontalAlignment="Center" Height="100" VerticalAlignment="Center" Width="100"/>
                            <Button Content="Button" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0" Height="100" Width="100" Click="Img_Click"/>
                            <Border BorderBrush="Black" BorderThickness="2" HorizontalAlignment="Center" Height="100" VerticalAlignment="Center" Width="100"/>
                        </Grid>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Phone" FontFamily="Segoe WP" FontSize="15"/>
                        <TextBox x:Name="Phone" PlaceholderText="Phone" Text="{Binding Phone, Mode=TwoWay}"/>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Country" FontFamily="Segoe WP" FontSize="15"/>
                        <TextBox x:Name="Country" PlaceholderText="Country" Text="{Binding Country, Mode=TwoWay}"/>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Linkedin" FontFamily="Segoe WP" FontSize="15"/>
                        <TextBox x:Name="Linkedin" PlaceholderText="Linkedin" Text="{Binding Linkedin, Mode=TwoWay}"/>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Viadeo" FontFamily="Segoe WP" FontSize="15"/>
                        <TextBox x:Name="Viadeo" PlaceholderText="Viadeo" Text="{Binding Viadeo, Mode=TwoWay}"/>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Twitter" FontFamily="Segoe WP" FontSize="15"/>
                        <TextBox x:Name="Twitter" PlaceholderText="Twitter" Text="{Binding Twitter, Mode=TwoWay}"/>
                        <StackPanel x:Name="PasswordStackPanel" Visibility="Collapsed" Margin="5,0,5,0">
                            <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Old Password" FontFamily="Segoe WP" FontSize="15"/>
                            <PasswordBox x:Name="oldPassword" BorderBrush="" VerticalAlignment="Stretch"/>
                            <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="New Password" FontFamily="Segoe WP" FontSize="15"/>
                            <PasswordBox x:Name="newPassword"  BorderBrush="" VerticalAlignment="Stretch"/>
                            <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="Retype Password" FontFamily="Segoe WP" FontSize="15"/>
                            <PasswordBox x:Name="retypePassword" BorderBrush="" VerticalAlignment="Stretch"/>
                        </StackPanel>
                        <Button Content="Change Password" HorizontalAlignment="Center" Click="Password_Click"/>
                        <Button Content="Update" Click="Update_Click" HorizontalAlignment="Center"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </slideMenu:SlideInMenuContentControl.Content>
    </slideMenu:SlideInMenuContentControl>
</Page>
