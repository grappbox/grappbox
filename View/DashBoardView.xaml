<Page
    x:Class="GrappBox.View.DashBoardView"
    xmlns:viewModel="using:GrappBox.ViewModel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GrappBox.View"
    xmlns:customControl="using:GrappBox.CustomControler"
    xmlns:slideMenu="using:GrappBox.CustomControler.SlidingMenu"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >
    <slideMenu:SlideInMenuContentControl x:Name="slideInMenuContentControl"  PageTitle="Dashboard">
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Pivot Name="db_pivot" Style="{StaticResource DefaultPivotStyle}" Margin="0,27,0,0">
                <PivotItem Background="{StaticResource BackgroundGrayBrush}" Margin="0,0,0,0" Header="Settings">
                    <StackPanel Margin="15,5,0,0">
                        <TextBlock FontSize="24" Foreground="Black">Displayed Pans</TextBlock>
                        <CheckBox Name="team_cb" Click="team_cb_Checked" Foreground="Black" BorderBrush="Black" RequestedTheme="Light">Team</CheckBox>
                        <CheckBox Name="meetings_cb" Click="meetings_cb_Checked" Foreground="Black" BorderBrush="Black" RequestedTheme="Light">Meetings</CheckBox>
                    </StackPanel>
                </PivotItem>
            </Pivot>
            <ProgressBar VerticalAlignment="Center" Name="LoadingBar" Visibility="Collapsed" IsEnabled="False" IsIndeterminate="true" Height="4" HorizontalAlignment="Stretch"/>
        </Grid>
    </slideMenu:SlideInMenuContentControl>
</Page>
