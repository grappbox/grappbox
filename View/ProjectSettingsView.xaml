<Page
    x:Class="GrappBox.View.ProjectSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GrappBox.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="using:GrappBox.ViewModel.DashBoardViewModel"
    xmlns:valueconverter="using:GrappBox.ViewModel"
    xmlns:slideMenu="using:GrappBox.CustomControler.SlidingMenu">

    <Page.BottomAppBar>
        <CommandBar Visibility="Collapsed" x:Name="CommandBar" ClosedDisplayMode="Compact">
            <AppBarButton Visibility="Collapsed" Name="RemoveUser" Label="Remove" Click="RemoveUserButton_Click" Icon="Delete"/>
            <AppBarButton Visibility="Collapsed" Name="RemoveCU" Label="Remove" Click="RemoveCustomerButton_Click" Icon="Delete"/>
            <AppBarButton Visibility="Collapsed" Name="RegenerateCu" Label="Regenerate" Click="RegenerateCU_Click" Icon="Sync"/>
        </CommandBar>
    </Page.BottomAppBar>

    <slideMenu:SlideInMenuContentControl x:Name="slideInMenuContentControl" Grid.Row="1" MenuState="Left" IsDoubleTapEnabled="False">
        <slideMenu:SlideInMenuContentControl.LeftMenuContent>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button Content="Dashboard" Click="Dashboard_Click" Grid.Row="0" HorizontalAlignment="Center"/>
                <Button Content="WhiteBoard" Click="Whiteboard_Click" Grid.Row="1" HorizontalAlignment="Center"/>
                <Button Content="User Settings" Click="UserSettings_Click" Grid.Row="2" HorizontalAlignment="Center"/>
                <Button Content="Project Settings" Click="ProjectSettings_Click" Grid.Row="3" HorizontalAlignment="Center"/>
            </Grid>
        </slideMenu:SlideInMenuContentControl.LeftMenuContent>
        <slideMenu:SlideInMenuContentControl.RightMenuContent>
            <Grid/>
        </slideMenu:SlideInMenuContentControl.RightMenuContent>
        <slideMenu:SlideInMenuContentControl.Content>
            <Pivot Name="Pivot" Title="Project Settings" Background="#FF0C0C0C" SelectionChanged="Pivot_SelectionChanged">
                <PivotItem Header="Settings">
                    <ScrollViewer>
                        <StackPanel>
                            <StackPanel.Resources>
                                <valueconverter:DateTimeToDateConverter x:Key="DateConverter"/>
                            </StackPanel.Resources>
                            <TextBlock Text="Name" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15"/>
                            <TextBox x:Name="Name" PlaceholderText="Name" Text="{Binding Name, Mode=TwoWay}"/>

                            <TextBlock Text="Description" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15"/>
                            <TextBox x:Name="Description" TextWrapping="Wrap" AcceptsReturn="True" PlaceholderText="Description" Text="{Binding Description, Mode=TwoWay}" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"/>

                            <TextBlock Text="Logo" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15"/>
                            <Grid>
                                <Image x:Name="img" Source="{Binding Logo, Mode=OneWay}" HorizontalAlignment="Center" Height="100" VerticalAlignment="Center" Width="100"/>
                                <Button Content="Button" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0" Height="100" Width="100" Click="Img_Click"/>
                                <Border BorderBrush="White" BorderThickness="2" HorizontalAlignment="Center" Height="100" VerticalAlignment="Center" Width="100"/>
                            </Grid>

                            <TextBlock Text="Phone" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15"/>
                            <TextBox x:Name="Phone" PlaceholderText="Phone" Text="{Binding Phone, Mode=TwoWay}"/>

                            <TextBlock Text="Company" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15"/>
                            <TextBox x:Name="Company" PlaceholderText="Company" Text="{Binding Company, Mode=TwoWay}"/>

                            <TextBlock Text="Contact mail" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15"/>
                            <TextBox x:Name="ContactMail" PlaceholderText="Contact mail" Text="{Binding ContactMail, Mode=TwoWay}"/>

                            <TextBlock Text="Facebook" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15"/>
                            <TextBox x:Name="Facebook" PlaceholderText="Facebook" Text="{Binding Facebook, Mode=TwoWay}"/>

                            <TextBlock Text="Twitter" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15"/>
                            <TextBox x:Name="Twitter" PlaceholderText="Twitter" Text="{Binding Twitter, Mode=TwoWay}"/>

                            <StackPanel x:Name="MoreStackPanel" Visibility="Collapsed" Margin="5,0,5,0">
                                <TextBlock Name="DeleteDate" Visibility="Collapsed" Foreground="Red" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15"/>
                                <Button Name="DeleteButton" HorizontalAlignment="Center" Click="Delete_Click"/>
                            </StackPanel>
                            <Button Name="MoreButton" Content="More" HorizontalAlignment="Center" Click="More_Click"/>
                            <Button Content="Update" Click="ProjectSettingsUpdate_Click" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </ScrollViewer>
                </PivotItem>
                <PivotItem Header="Users">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="8.5*"/>
                            <RowDefinition Height="0.5*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0">
                            <ListBox x:Name="userListView" ItemsSource="{Binding UserList}" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" Background="{x:Null}" Foreground="White" SelectionChanged="userListView_SelectionChanged">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Firstname}" Grid.Column="0" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" />
                                            <TextBlock Text="{Binding Lastname}" Grid.Column="1" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" />
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="8*"/>
                                    <ColumnDefinition Width="2*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="roland.hemmer@epitech.eu" x:Name="UserMail" Grid.Column="0" PlaceholderText="Email"/>
                                <Button Content="Add" Click="AddUser_Click" Grid.Column="1" HorizontalAlignment="Center" Width="70" Height="50" MinWidth="70" MinHeight="60"/>
                            </Grid>
                        </StackPanel>
                        <TextBlock x:Name="infoBlock" Grid.Row="1" TextAlignment="Center" VerticalAlignment="Center" Foreground="#FFE81123"  FontFamily="Segoe WP" FontSize="15"/>
                    </Grid>
                </PivotItem>
                <PivotItem Header="Customer access">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="8.5*"/>
                            <RowDefinition Height="0.5*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0">
                            <ListBox x:Name="CustomerListView" ItemsSource="{Binding CustomerList}" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" Background="{x:Null}" Foreground="White" SelectionChanged="customerListView_SelectionChanged">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Name}" Grid.Column="0" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" />
                                            <TextBlock Text="{Binding Token}" Grid.Column="1" TextWrapping="Wrap" FontFamily="Segoe WP" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" />
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="8*"/>
                                    <ColumnDefinition Width="2*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="Yolo" x:Name="CustomerName" Grid.Column="0" PlaceholderText="Customer name"/>
                                <Button Content="Add" Click="AddCU_Click" Grid.Column="1" HorizontalAlignment="Center" Width="70" Height="50" MinWidth="70" MinHeight="60"/>
                            </Grid>
                        </StackPanel>
                        <TextBlock x:Name="errorBlock" Grid.Row="1" TextAlignment="Center" VerticalAlignment="Center" Foreground="#FFE81123"  FontFamily="Segoe WP" FontSize="15"/>
                    </Grid>
                </PivotItem>
            </Pivot>
        </slideMenu:SlideInMenuContentControl.Content>
    </slideMenu:SlideInMenuContentControl>
</Page>
