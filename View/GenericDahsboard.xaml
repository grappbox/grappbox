<Page
    x:Class="GrappBox.View.GenericDahsboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GrappBox.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:customControl="using:GrappBox.CustomControler"
    xmlns:slideMenu="using:GrappBox.CustomControler.SlidingMenu"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <slideMenu:SlideInMenuContentControl  x:Name="slideInMenuContentControl" PageTitle="Dashboard" IsDoubleTapEnabled="False">
        <Grid>
            <ListView
            Padding="15,0,15,0"
            ItemsSource="{Binding ProjectList}"
            Background="{StaticResource BackgroundGrayBrush}"
            SelectionChanged="ListView_SelectionChanged"
            ScrollViewer.HorizontalScrollMode="Disabled"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ScrollViewer.VerticalScrollMode="Auto"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            HorizontalAlignment="Stretch"
            HorizontalContentAlignment="Center">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="15" Background="White">
                            <StackPanel>
                                <StackPanel.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="16"/>
                                    </Style>
                                </StackPanel.Resources>
                                <TextBlock Name="hidden" Text="{Binding Id, Converter={StaticResource IntegerToStringConverter}}" Visibility="Collapsed"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <Image HorizontalAlignment="Left" Width="100" Height="100" Source="{Binding Img}" />
                                    <TextBlock Name="projectName" VerticalAlignment="Center" HorizontalAlignment="Stretch" TextAlignment="Center" FontWeight="ExtraBold" Text="{Binding Name}" Margin="20,0,0,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <StackPanel.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Margin" Value="10,0,0,0"/>
                                        </Style>
                                    </StackPanel.Resources>
                                    <Image Source="/Assets/bug.png"/>
                                    <TextBlock Text="{Binding Bugs}"/>
                                    <TextBlock Text="bugs"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <StackPanel.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Margin" Value="10,0,0,0"/>
                                        </Style>
                                    </StackPanel.Resources>
                                    <Image Source="/Assets/chat.png"/>
                                    <TextBlock Text="{Binding Messages}"/>
                                    <TextBlock Text="messages"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <StackPanel.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Margin" Value="10,0,0,0"/>
                                        </Style>
                                    </StackPanel.Resources>
                                    <Image Source="/Assets/task_ongoing.png"/>
                                    <TextBlock Text="{Binding OngoingTasks}"/>
                                    <TextBlock Text="/" Margin="0"/>
                                    <TextBlock Text="{Binding TotalTasks}" Margin="0"/>
                                    <TextBlock Text="finished tasks"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="15"></Setter>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
            <ProgressBar VerticalAlignment="Center" x:Name="LoadingBar" Visibility="Collapsed" IsEnabled="False" IsIndeterminate="true" Height="4" HorizontalAlignment="Stretch"/>
        </Grid>
    </slideMenu:SlideInMenuContentControl>
</Page>