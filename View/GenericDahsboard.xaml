<Page
    x:Class="GrappBox.View.GenericDahsboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GrappBox.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:customControl="using:GrappBox.CustomControler"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.BottomAppBar>
        <CommandBar x:Name="CB" ClosedDisplayMode="Compact">
            <AppBarButton Name="CreateProject" Label="New Project" Click="CreateProject_Click" Icon="Add"/>
        </CommandBar>
    </Page.BottomAppBar>
    <SplitView DisplayMode="Inline" x:Name="Menu" OpenPaneLength="200">
        <SplitView.Pane>
            <customControl:Menu/>
        </SplitView.Pane>
        <SplitView.Content>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="65"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel x:Name="Menubar" ManipulationMode="TranslateX" Grid.Row="0" Orientation="Horizontal" Background="{StaticResource RedGrappboxBrush}">
                    <Button x:Name="MenuButton" Width="48" Height="48" MinHeight="48" MinWidth="48" Background="Transparent" BorderBrush="Transparent" Style="{StaticResource MenuButtonStyle}" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" Foreground="White" Click="MenuButton_Click"/>
                    <TextBlock Margin="15,0,0,0" FontSize="24" Text="Dashboard" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Left" />
                </StackPanel>
                <ListView
                    Grid.Row="1"
                    Padding="15,0,15,0"
                    ItemsSource="{Binding ProjectList}"
                    SelectionChanged="ListView_SelectionChanged"
                    ScrollViewer.HorizontalScrollMode="Disabled"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.VerticalScrollMode="Auto"
                    ScrollViewer.VerticalScrollBarVisibility="Hidden"
                    HorizontalAlignment="Stretch" IsDoubleTapEnabled="False" IsRightTapEnabled="False">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <StackPanel.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="16"/>
                                    </Style>
                                </StackPanel.Resources>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <Image HorizontalAlignment="Left" Width="100" Height="100" Source="{Binding Logo}" />
                                    <TextBlock Name="projectName" VerticalAlignment="Center" HorizontalAlignment="Stretch" TextAlignment="Center" FontWeight="ExtraBold" Text="{Binding Name}" Margin="20,0,0,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <StackPanel.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Margin" Value="10,0,0,0"/>
                                        </Style>
                                    </StackPanel.Resources>
                                    <Image Source="/Assets/bug.png"/>
                                    <TextBlock Text="{Binding Bugs}"/>
                                    <TextBlock Text="bugs"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <StackPanel.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Margin" Value="10,0,0,0"/>
                                        </Style>
                                    </StackPanel.Resources>
                                    <Image Source="/Assets/chat.png"/>
                                    <TextBlock Text="{Binding Messages}"/>
                                    <TextBlock Text="messages"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                    <StackPanel.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Margin" Value="10,0,0,0"/>
                                        </Style>
                                    </StackPanel.Resources>
                                    <Image Source="/Assets/task_ongoing.png"/>
                                    <TextBlock Text="{Binding OngoingTasks}"/>
                                    <TextBlock Text="/" Margin="0"/>
                                    <TextBlock Text="{Binding TotalTasks}" Margin="0"/>
                                    <TextBlock Text="finished tasks"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="White" />
                            <Setter Property="Padding" Value="15" />
                            <Setter Property="Margin" Value="15" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="HorizontalAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
                <ProgressBar VerticalAlignment="Center" x:Name="LoadingBar" Visibility="Collapsed" IsEnabled="False" IsIndeterminate="true" Height="4" HorizontalAlignment="Stretch"/>
            </Grid>
        </SplitView.Content>
    </SplitView>
</Page>